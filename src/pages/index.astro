---
// Root redirect based on browser language preference
const browserLang = Astro.request.headers.get('accept-language')
const preferredLang = browserLang?.toLowerCase().includes('es') ? 'es' : 'en'

return Astro.redirect(`/${preferredLang}`)
---
